mixin episode(speaker, description, keywords, number, image, url, variantInside, variantTitle)
  section(class= variantInside).intro-episode
    if variantInside == '-inside'
      .cover-wrap
        .cover
          img(src='/img/' + image alt='Fotografia de ' + speaker)
      .text
        h2(class= variantTitle).title= speaker
        p.keywords= description
        p.keywords= keywords
      span.number= number
    else
      .cover-wrap
        .cover
          a(href= url title='Ir para episódio').play-badge
            img(src='/img/' + image alt='Fotografia de ' + speaker)
            svg(src='/img/play.svg')
      .text
        h2(class= variantTitle).title= speaker
        p.keywords= description
        p.keywords= keywords
      span.number= number
